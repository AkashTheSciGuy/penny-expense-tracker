# Server-side only. Use the POOLED Neon connection string.
# Netlify: Project configuration -> Environment variables -> DATABASE_URL
# Never prefix this with VITE_ and never expose it to browser code.
DATABASE_URL=postgresql://USER:PASSWORD@YOUR-ENDPOINT-pooler.REGION.aws.neon.tech/neondb?sslmode=require
